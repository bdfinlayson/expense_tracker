<%= form_for @form do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: 'Vendor name' %>
  <%= f.label :note %>
  <%= f.text_area :note, placeholder: 'Notes on this vendor here' %>
  <%= f.submit %>
<% end %>

