<%= form_for @form do |f| %>
  <%= f.text_field :amount, type: :number, step: '0.01' %>
  <%= collection_select(:expense, :category_id, Category.all, :id, :name, prompt: true) %>
  <%= collection_select(:expense, :vendor_id, Vendor.all, :id, :name, prompt: true) %>

  <%= f.submit %>
<% end %>
