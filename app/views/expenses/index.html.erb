<h1 class='page-header'>
  <span>
    Expenses (<%= @total_items %>)
    <%= render partial: 'modal/show',
      locals: {
        form: 'form/show',
        modal_trigger: 'Create New +',
        modal_title: 'New Expense',
        modal_id: 'new',
        model: @form
      }
    %>
  </span>
  <%= render partial: 'expenses/stats' %>

</h1>

<%= render partial: 'table/show', locals: {
  columns: @columns,
  collection: @expenses
  }
%>
