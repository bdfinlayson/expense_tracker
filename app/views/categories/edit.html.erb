<%= form_for @form do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= collection_select(:category, :ancestry, Category.all, :id, :name, prompt: 'Select a parent category', selected: @parent_id) %>
  <%= f.submit %>
<% end %>

